# ================================================================
# RAG Agentic AI Internal Audit System - UPDATED REQUIREMENTS
# Python 3.11+ | Enhanced Security & Performance | January 2025
# ================================================================

# -- Core Framework --
streamlit==1.39.0
python-dotenv==1.0.1

# -- AI & ML Stack (Updated for better performance) --
# OpenAI client - latest stable
openai==1.12.0

# Vector database - latest stable versions
chromadb==0.4.24
sentence-transformers==2.3.1

# Embedding models with better performance
transformers==4.37.2
huggingface-hub==0.20.3

# Fix protobuf conflicts (CRITICAL)
protobuf==3.20.3

# -- Database & Backend --
# Firebase - latest stable
firebase-admin==6.5.0

# -- Data Analysis & Visualization --
pandas==2.2.2
numpy==1.26.4
plotly==5.24.1

# -- Document Processing (Enhanced) --
python-docx==1.1.2
PyPDF2==3.0.1

# NEW: Advanced document processing
pymupdf==1.24.1          # Better PDF processing
pillow==10.2.0           # Image processing
python-magic==0.4.27     # File type detection

# -- Security & Validation (NEW) --
pydantic==2.6.1          # Data validation
bleach==6.1.0            # HTML sanitization
cryptography==42.0.2     # Encryption utilities

# -- Performance & Monitoring (NEW) --
psutil==5.9.8            # System monitoring
tenacity==8.2.3          # Retry mechanisms
structlog==24.1.0        # Better logging

# -- Compatibility Fixes --
torch==2.1.2+cpu         # CPU-only for better compatibility
tokenizers==0.15.1
urllib3==2.1.0
requests==2.31.0

# -- Development & Testing (Optional) --
pytest==8.0.0
pytest-asyncio==0.23.4
black==24.1.1
mypy==1.8.0

# -- Production Deployment (Optional) --
gunicorn==21.2.0         # Production server
uvicorn==0.27.0          # ASGI server
fastapi==0.109.0         # API framework (for future expansion)